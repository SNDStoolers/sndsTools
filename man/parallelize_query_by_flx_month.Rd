% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{parallelize_query_by_flx_month}
\alias{parallelize_query_by_flx_month}
\title{Exécute une fonction de construction de requête par mois de flux}
\usage{
parallelize_query_by_flx_month(
  conn,
  start_date,
  end_date,
  query_builder_function,
  query_builder_kwargs,
  dis_dtd_lag_months = 6,
  r_cluster_cores = 1
)
}
\arguments{
\item{conn}{Connexion DBI à la base de données.}

\item{start_date}{Date. Date de début de la période d'extraction.}

\item{end_date}{Date. Date de fin de la période d'extraction.}

\item{query_builder_function}{Fonction qui construit la requête SQL pour un
mois donné. Exemple : \code{\link{.extract_drug_by_month}} depuis le
fichier \code{R/extract_drug_erprsf.R}.}

\item{query_builder_kwargs}{Liste d'arguments supplémentaires transmis à la
fonction de construction. Elle contient typiquement les paramètres globaux
comme \code{sup_columns}, \code{output_table_name}, etc.}

\item{dis_dtd_lag_months}{Entier. Nombre de mois de retard pris en compte
pour la date \code{FLX_DIS_DTD}. Valeur par défaut : 6.}

\item{r_cluster_cores}{Entier. Nombre de cœurs à utiliser pour le
parallélisme.
Valeur par défaut : 1 (exécution séquentielle).}
}
\value{
Invisible NULL. La fonction orchestre l'exécution de la fonction de
construction de requête pour chaque mois, en créant ou insérant les
résultats dans la table de sortie.
}
\description{
Cette fonction découpe la période d'extraction en mois de flux,
prépare les paramètres spécifiques à chaque mois et invoque la fonction
\code{query_builder_function} soit en mode séquentiel, soit en parallèle via le
package \code{parallel}. Elle gère la création de la table de sortie lors du
premier mois.
}
\details{
La fonction construit une liste \code{months_to_process} contenant pour
chaque mois les paramètres \code{dis_dtd_year}, \code{dis_dtd_month},
\code{is_first_month}, \code{formatted_start_date}, \code{formatted_end_date},
\code{end_year} ainsi que les arguments fournis dans \code{query_builder_kwargs}.
Elle crée ensuite un cluster si \code{r_cluster_cores} > 1, exporte les packages
nécessaires et la connexion, exécute le premier mois pour créer la table,
puis traite les mois restants en parallèle avec \code{parallel::parLapply}.
}
\examples{
\dontrun{
parallelize_query_by_flx_month(
  conn = conn,
  start_date = as.Date("2020-01-01"),
  end_date = as.Date("2020-03-31"),
  query_builder_function = .extract_drug_by_month,
  query_builder_kwargs = list(
    sup_columns = NULL,
    output_table_name = "TMP_DISP",
    show_sql_query = FALSE
  )
)
}
}
\seealso{
Other utils: 
\code{\link{connect_duckdb}()},
\code{\link{connect_oracle}()},
\code{\link{constants_snds}()},
\code{\link{create_table_from_query}()},
\code{\link{gather_table_stats}()},
\code{\link{get_first_non_archived_year}()},
\code{\link{insert_into_table_from_query}()},
\code{\link{retrieve_all_psa_from_idt}()},
\code{\link{retrieve_all_psa_from_psa}()}
}
\concept{utils}
