% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_drug_erprsf.R
\name{.extract_drug_by_month}
\alias{.extract_drug_by_month}
\title{Fonction pour traiter un mois de délivrances de médicaments}
\usage{
.extract_drug_by_month(kwargs)
}
\arguments{
\item{kwargs}{Liste. Éléments :
\itemize{
\item \code{dis_dtd_year} entier. Année du flux FLX_DIS_DTD.
\item \code{dis_dtd_month} entier. Mois du flux FLX_DIS_DTD.
\item \code{is_first_month} logique. \code{TRUE} pour le premier mois (crée la table
de sortie), \code{FALSE} pour les mois suivants (insère les lignes).
\item \code{formatted_start_date} caractère. Date de début de la période (YYYY‑MM‑DD).
\item \code{formatted_end_date} caractère. Date de fin de la période (YYYY‑MM‑DD).
\item \code{end_year} entier. Année de fin de la période.
\item \code{output_table_name} caractère. Nom de la table de destination.
\item \code{show_sql_query} logique. Si \code{TRUE}, la requête SQL du premier mois est journalisée.
\item \code{ir_pha_r_filtered_name} caractère. Nom de la table temporaire contenant
les lignes filtrées de \code{IR_PHA_R}.
\item \code{sup_columns} vecteur de caractères. Colonnes supplémentaires à conserver.
\item \code{patients_ids_table_name} caractère ou \code{NULL}. Table temporaire avec les IDs patients.
\item \code{conn} connexion DBI. Connexion à la base (peut être \code{NULL} en workers parallèles).
}}
}
\value{
Invisible \code{NULL}. La fonction crée ou ajoute des lignes à
\code{output_table_name} dans la base Oracle.
}
\description{
Cette fonction est appelée par
\code{\link{extract_drug_erprsf}} via
\code{\link[parallelize_query_by_flx_month]} pour construire et exécuter la
requête SQL d'un mois de flux. Elle reçoit une liste de paramètres (\code{kwargs})
contenant les valeurs spécifiques au mois et les autres paramètres de la fonction principale (noms des tables filtres, nom de la table de sortie, ...).
}
\details{
La fonction construit une requête joignant les tables de
prescription et de délivrance, applique les filtres médicaments, les filtres
patients et les colonnes additionnelles éventuelles, puis crée la table de
sortie (premier mois) ou y insère les lignes (mois suivants).
}
\seealso{
Other extract: 
\code{\link{extract_consultations_erprsf}()},
\code{\link{extract_drug_erprsf}()},
\code{\link{extract_hospital_consultations}()},
\code{\link{extract_hospital_stays}()},
\code{\link{extract_long_term_disease}()}
}
\concept{extract}
